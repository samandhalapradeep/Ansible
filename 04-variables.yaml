- name: variables from PLAY level
  hosts: localhost
  vars:
    course: "Devops"
    Trainer: "Prdaeep"
    duration: "110 hrs"
  tasks:
    - name: check variables
      ansible.builtin.debug:
        msg: "hi im learning {{course}}, trainer is {{Trainer}}, duration is {{}}"